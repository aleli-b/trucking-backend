services:
  - type: web
    plan: free
    name: spotter
    runtime: python
    buildCommand: './build.sh'
    startCommand: 'python -m gunicorn spotter.asgi:application -k uvicorn.workers.UvicornWorker'
    envVars: